"""Observation runtime processing package.

Owns the Observation Window's read-only ROI engine + alarm runtime
(observation.runtime) and the ROI overlay renderer (observation.overlay).
"""

from __future__ import annotations

__version__ = "1.0.0"
