# Development / test dependencies for the Thermal Monitoring System v2.
# Installed inside .venv by setup_environment.py after requirements.txt.
#
# PyQt6 is used only by diagnostic and test GUIs (tests/*, halcon_* tools).
# The main application uses PyQt5 (see requirements.txt), so PyQt6 and
# PySide6 are never runtime dependencies.

pytest==9.1.1
ruff==0.15.22
PyQt6==6.11.0
debugpy==1.8.21
psutil==7.2.2
pyodbc==5.3.0                # only used by halcon_roi_validation.py (optional tool)
