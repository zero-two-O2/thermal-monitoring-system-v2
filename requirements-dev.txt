# Development dependencies for the project virtual environment.
#
# IMPORTANT:
# - HALCON (mvtec-halcon) is NOT listed here. It is a global dependency
#   provided by the global Python installation (see docs/Development_Setup.md).
# - This virtual environment must be created with --system-site-packages
#   so that global packages (HALCON, numpy, opencv, PyQt5) remain visible.
# - Packages provided by the global installation: numpy, opencv-python,
#   mvtec-halcon, harvesters, genicam, PyQt5.

PyQt6==6.11.0
pytest==9.1.1
ruff==0.15.22
debugpy==1.8.21
psutil==7.2.2
halcon
numpy
pyodbc