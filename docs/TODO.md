# Put calibratiom bob in the assets/calibration ✓
# ok. tell me the improvementa and exact loction where to make them. also i thing i want to add. so every camera has a unique serial no now i dont want tha cameras that open first to take first place tile in the GUI windows. what i want is we will sane the camera IP , serial no, and other details in Db and assign each camera a no. so each no camera will be given a spacific tile in the GUI window. i this correct. tell me waht changes to make for this and other changes
# Okay so the frames are moving in a single line like army making copies of the frame or something like that because somewhere in the code I saw copy functions and something like that. I got miss a concept like in the Apple or ios chips there is a concept of integrated memory like the CPU and GPU shares the same memory. So unlike traditional CPU and GPU CPU and GPU have to send data with each other dissure memory reduces the memory transfer can we apply this concept in this project anywhere. Not just talking about data transfer between CPU and GPU I am just want to apply this concert
# 3. Graphics → Text
        Very useful.
        Instead of OpenCV text:
        Motor A
        73.4°C
        Alarm
        HALCON can draw
        Temperature
        ROI name
        Camera name
        FPS
        Time
        Alarm status
        Operators generally look at this more than the ROI itself.
        4. Graphics → Window
        Very important.
        Examples:
        Multiple image windows
        Zoom
        Pan
        Window resize
        Double buffering
        Faster display
        i want to use this in our project. is it the right time to make this changes. 
        if yes, then what detailes do you need for this from me

#